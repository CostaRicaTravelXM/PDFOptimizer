/**
 * Bundled by n8n/build.mjs so the workflow JSON is generated from the same constants the
 * compiler uses. Nothing here runs at request time.
 */
export {
  COPY_LIMITS,
  DEFAULT_LAYOUT_FOR_TYPE,
  LAYOUT_IDS,
  SLIDE_LIMITS,
  SLIDE_TYPES,
} from '@/lib/presentations/manifest';
export { MANIFEST_JSON_SCHEMA } from '@/lib/presentations/manifestSchema';
export { LIMITS } from '@/lib/presentations/types';
export { BRAND_PALETTE_HEX, FONT_WHITELIST, FONTS } from '@/lib/server/presentations/brand';
